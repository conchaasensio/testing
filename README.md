This project was created to train testing and TDD.

## Available Scripts

In the project directory, you can run:

### `npm install`

To install all the necesary dependencies.

### `npm test`

Launches the test runner in the interactive watch mode.
